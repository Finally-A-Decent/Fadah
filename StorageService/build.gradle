repositories {
}

dependencies {
    implementation project(":StorageProto")
}

jar {
    manifest {
        attributes(
                'Main-Class': 'info.preva1l.fadah.StorageService'
        )
    }
}

shadowJar {
    destinationDirectory.set(file("$rootDir/target"))
    minimize()
}